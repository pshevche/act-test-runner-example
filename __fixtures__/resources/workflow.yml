name: echo-comment-test
on:
  issue_comment:
    types: [created]

jobs:
  echo-comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./
        with:
          github-token: fake-token
